// This file is replaced by `pnpm run build:elm`.
const appNode = document.getElementById('app');

if (appNode) {
	appNode.innerHTML =
		'<main class="mx-auto max-w-3xl px-4 py-8"><p class="text-gray-700">Baue die Elm-Anwendung mit <code>pnpm run build:app</code>, um das interaktive Handbuch zu laden.</p></main>';
}
